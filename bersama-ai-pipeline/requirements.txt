# BersamaAi summarization pipeline — v1
# Install with:  python -m pip install -r requirements.txt
openai>=1.66            # Responses API client; pointed at LLM_BASE_URL (OpenCode Go today)
youtube-transcript-api>=1.2.4   # instance .fetch() API (v1.2.4, Jan 2026); legacy classmethod still works
yt-dlp>=2026.7.1
requests>=2.32
python-dotenv>=1.0

# Audio transcription fallback (videos without captions) — Groq Whisper
groq>=0.11
# Pillow>=10.0          # card image generation
