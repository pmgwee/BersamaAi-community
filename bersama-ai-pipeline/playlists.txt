# BersamaAi creator-watch — YouTube channels to watch for NEW uploads.
# One channel URL per line. Lines starting with # are ignored. Any text after #
# on a line is also treated as a comment (so "URL  # note" is fine). Blank lines ignored.
#
# The daily scan summarizes only uploads from the last ~3 days (NEW videos), not the
# channel backlog. Already-summarized videos are deduped (state/processed.json).
# Caption-less videos are transcribed via Groq Whisper (if GROQ_API_KEY is set).
#
# Add more creators any time — one URL per line:
https://www.youtube.com/@kellytsaii          # Kelly Tsai — tech / CS careers
https://www.youtube.com/@lingdujieshuo        # 零度解说 — viral AI news & reviews
